# 싱글스레드
- 자바스크립트는 싱글스레드
- 한 번에 하나의 일 밖에 못함
	- 만약 오래걸리는 일이라면?
	- 다른 작업들은 해당 작업이 끝나기를 기다려야함
- 이를 해결하기 위해 일부 작업은 비동기로 수행함
	1. Callback 함수
	2. Promise
	3. Async, Await
# Callback 함수
- 특정 함수에 매개변수로 전달된 함수
- 함수를 전달받은 함수 안에서 호출됨
## 단점
1. 소스 코드를 보는데 가독성이 떨어짐
2. 에러 처리를 한다면 모든 콜백에서 각각 에러 핸들링을 해줘야 함