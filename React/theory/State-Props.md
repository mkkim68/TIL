# State와 Props
## State
1. 부모 컴포넌트에서 자녀 컴포넌트로 데이터를 보내는게 아닌 해당 컴포넌트 내부에서 데이터를 전달
2. 변경 가능 (mutable)
3. 변하면 re-render됨
## Props
1. Properties의 줄임말
2. 상속하는 부모 컴포넌트로부터 자녀 컴포넌트에 데이터들을 전달하는 방법
3. 읽기 전용(immutable)으로 자녀 컴포넌트 입장에서는 변하지 않는다